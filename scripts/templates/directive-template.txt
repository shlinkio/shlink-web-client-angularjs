
(function () {
    'use strict';

    angular
        .module('%module%')
        .directive('%directive%', [
            %directive%
        ]);

    function %directive% () {
        return {
            templateUrl: '',
            link: function (scope, element) {

            }
        };
    }
})();
