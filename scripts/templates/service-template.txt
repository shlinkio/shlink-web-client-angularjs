
(function () {
    'use strict';

    angular
        .module('%module%')
        .factory('%service%', [
            %service%
        ]);

    function %service% () {
        return {

        };
    }
})();
